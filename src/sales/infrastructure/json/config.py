from pathlib import Path

_JSON_FOLDER_NAME = "crm-ddd-json-db"

LEADS_JSON_FILE_PATH = Path.home() / _JSON_FOLDER_NAME / "leads.json"
NOTES_JSON_FILE_PATH = Path.home() / _JSON_FOLDER_NAME / "notes.json"
ASSIGNMENTS_JSON_FILE_PATH = Path.home() / _JSON_FOLDER_NAME / "lead-assignments.json"
